import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const students=sqliteTable('students',{id:text('id').primaryKey(),name:text('name').notNull(),nameKey:text('name_key').notNull(),passwordHash:text('password_hash').notNull(),saveJson:text('save_json').notNull(),revision:integer('revision').notNull().default(0),createdAt:integer('created_at').notNull(),updatedAt:integer('updated_at').notNull()},t=>[uniqueIndex('students_name_key_unique').on(t.nameKey)]);
export const sessions=sqliteTable('sessions',{tokenHash:text('token_hash').primaryKey(),studentId:text('student_id').references(()=>students.id,{onDelete:'cascade'}),role:text('role').notNull(),expiresAt:integer('expires_at').notNull()},t=>[index('sessions_expiry').on(t.expiresAt)]);
export const classroom=sqliteTable('classroom',{id:text('id').primaryKey(),settingsJson:text('settings_json').notNull()});
export const attempts=sqliteTable('auth_attempts',{key:text('key').primaryKey(),count:integer('count').notNull(),resetAt:integer('reset_at').notNull()});
export const storedFiles=sqliteTable('stored_files',{id:text('id').primaryKey(),objectKey:text('object_key').notNull(),originalName:text('original_name').notNull(),size:integer('size').notNull(),summaryJson:text('summary_json').notNull(),createdAt:integer('created_at').notNull()},t=>[uniqueIndex('stored_files_key_unique').on(t.objectKey)]);
export const chapterResources=sqliteTable('chapter_resources',{id:text('id').primaryKey(),course:text('course').notNull(),chapter:integer('chapter').notNull(),kind:text('kind').notNull(),fileId:text('file_id').notNull().references(()=>storedFiles.id),visible:integer('visible').notNull().default(0),updatedAt:integer('updated_at').notNull()},t=>[uniqueIndex('chapter_resource_slot_unique').on(t.course,t.chapter,t.kind)]);
export const submissions=sqliteTable('submissions',{id:text('id').primaryKey(),studentId:text('student_id').notNull().references(()=>students.id,{onDelete:'cascade'}),course:text('course').notNull(),chapter:integer('chapter'),title:text('title').notNull(),description:text('description').notNull(),projectUrl:text('project_url').notNull().default(''),fileId:text('file_id').references(()=>storedFiles.id),feedback:text('feedback').notNull().default(''),reviewed:integer('reviewed').notNull().default(0),createdAt:integer('created_at').notNull(),updatedAt:integer('updated_at').notNull(),revision:integer('revision').notNull().default(0)},t=>[index('submissions_student_updated').on(t.studentId,t.updatedAt),index('submissions_updated').on(t.updatedAt)]);
