# SCRATCH QUEST — GitHub用ソースコード

自由制作・作品提出・先生のコメント・章ごとの素材／答えsb3配布まで含むソース一式です。画像、教材、依存関係の固定ファイル、データベース作成手順を同梱しています。

## GitHubに置く

1. ZIPを展開してください。
2. GitHubで新しいリポジトリを作成してください。
3. `SCRATCH-QUEST` フォルダーの中身をアップロードしてください。隠しファイルの `.gitignore` と `.openai/hosting.json` も含めます。Gitを使う場合は、このフォルダーで `git init` して追加・コミットし、作成したリポジトリへpushできます。

元サイトの生徒情報・提出作品・先生のパスワードや秘密設定・Git履歴は含めていません。新しい環境では空のデータベースから始まります。元サイト固有の識別子も取り除いています。

## 手元で動かす

Node.js 22.13以上とnpmを用意し、このフォルダーで順番に実行します。

```sh
npm ci
node scripts/setup-teacher.mjs
npx wrangler d1 execute DB --local --config wrangler.local.json --persist-to .wrangler/state --file drizzle/0000_brown_komodo.sql
npx wrangler d1 execute DB --local --config wrangler.local.json --persist-to .wrangler/state --file drizzle/0001_black_gladiator.sql
npm run dev
```

表示されたlocalhostのURLを開きます。先生ログインには設定時に入力したパスワードを使います。生徒は名前とパスワードで新規登録できます。SQLの実行は新しい環境で一度だけ行ってください。

`.dev.vars`にはパスワードのハッシュを保存します。このファイルと`.wrangler`（ローカル生徒データ・ファイル）はGit管理対象外です。アップロードしないでください。

```sh
npx tsc --noEmit
npm run build
```

で型検査と公開用ビルドができます。

## インターネットで動かす

**GitHubにソースを公開することと、アプリを稼働させることは別です。GitHub Pagesだけでは登録・ログイン・作品保存は動きません。**

現在の構成はVinext / Cloudflare Workers、D1（生徒情報・進捗・提出内容）、R2（sb3）を使用しています。GitHub Pages向けの静的HTML版ではありません。

Sitesを使用する場合は、このソースから新しいサイトを作成し、D1の論理名`DB`、R2の論理名`FILES`を使用します。先生用秘密設定`TEACHER_PASSWORD_HASH`を新しいサイトに設定してください。値は設定スクリプトが作った`.dev.vars`の引用符内のハッシュです。Sitesでは同梱のSQLマイグレーションが公開時に適用されます。

別のCloudflareアカウントへ直接移設する場合は、そのアカウントのD1・R2作成、実リソースとのバインディング、秘密設定、SQL適用、Workers公開の設定が別途必要です。同梱の`wrangler.local.json`はローカル初期化専用で、そのまま本番公開に使用しないでください。汎用サーバーやVercel向けにそのまま動く構成ではありません。

ソースの細かな構成や機能の説明は`README.md`を参照してください。このZIPの移設・設定用追加ファイルは構文確認済みです。新しいアカウントへの公開は実施していません。
